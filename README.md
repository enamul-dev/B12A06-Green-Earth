1) What is the difference between var, let, and const?
Ans. Var is Function-scoped or globally scoped,  let is Block-scoped, const is Block-scoped.

2) Difference between map(), forEach(), and filter()
Ans.
map()Transforms each item.

forEach() Loops through array.

filter() Returns a new array.

3) What are arrow functions in ES6?

Ans. a concise alternative to traditional function expressions

4) How does destructuring assignment work in ES6?

Ans. allows you to unpack values from arrays

5) Explain template literals in ES6. How are they different from string concatenation?

Ans. Template literals, introduced in ES6, are a modern way to create strings in JavaScript using backticks (`) instead of single or double quotes. They offer significant advantages over traditional string concatenation, primarily by providing built-in support for string interpolation and multi-line strings.